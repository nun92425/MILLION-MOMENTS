# ステレオグラム（Stereogram / Autostereogram / SIRDS）技術仕様書

## MILLION MOMENTS — Stereogram Generator

**対象ファイル:** `src/stereogram.js:1` / `index.html:343` / `src/style.css:1` / `vite.config.js:1`  
**ホスティング:** Render Static Site（`render.yaml:1` / 完全ブラウザ完結・サーバーレス）  
**出力:** `1000×600px` PNG（`stereogram-canvas:1` / `canvas.toBlob('image/png'):1`）

---

## 1. 概要と分類学的定位

本ツールが生成するステレオグラムは、**ウォールペーパー・ステレオグラム（Wallpaper Stereogram）** および **SIRDS（Single-Image Random-Dot Stereogram / 単一画像ランダムドット・ステレオグラム）** に分類される **自己立体視画像（Autostereogram）** である。

*   **SIRDS:** B. Julesz（1960）のランダムドット・ステレオグラムを C. Tyler（1979）が単一画像化したもの。外部の立体眼鏡を要さず、**両眼視差（Binocular Disparity）** のみで奥行きを復元する。
*   **ウォールペーパー錯視:** 反復タイルの水平周期 `T` が網膜上で輻輳（Vergence）によって `T ± Δ` として知覚され、Δが視差として解釈される。本ツールでは `T = patternWidth`、`Δ = shift(depth)` とした **視差変調型ウォールペーパー・ステレオグラム** を実装している。

---

## 2. 視覚生理学的基盤

### 2.1 両眼立体視（Binocular Stereopsis）

ヒトの左右眼は平均瞳孔間距離（IPD: Inter-Pupillary Distance）約 63mm 離れており、同一注視点に対して **水平視差（Horizontal Disparity / Parallax）** が生じる。視差 `δ` と奥行き `Z` の関係は三角測量で

```
δ = (b * f) / Z
b: 基線長（IPD）, f: 焦点距離（眼球光学系）
```

に近似され、脳の第一次視覚野（V1）の **視差選択的神経細胞（Disparity-tuned Neurons）** が δ を抽出し、V2/V3で面の補間（Surface Interpolation）が行われる。

### 2.2 輻輳と調節の乖離（Vergence-Accommodation Conflict）

通常の実物体では **輻輳（Vergence / 両眼の内転）** と **調節（Accommodation / 水晶体の厚み変化）** が連動するが、ステレオグラムではスクリーン面（調節距離は一定）にピントを合わせつつ、輻輳のみを無限遠（平行法）または手前（交差法）に外すことで、 **Panumの融合領域（Panum's Fusional Area, 水平±15分程度）** 内で対応点探索が破綻し、反復パターンが奥行きとして再解釈される。

### 2.3 対応問題（Correspondence Problem）

反復パターンのどの周期が対応点かが曖昧なため、脳は **連続性拘束（Continuity Constraint）** と **一意性拘束（Uniqueness Constraint, Marr & Poggio 1976）** を用いて最も滑らかな深度面を選択する。本ツールでは深度マップの `lum` が連続的に変化するため、滑らかな曲面が知覚されやすい。一方、キャラクターのような意味的輪郭（Semantic Contour）は腹側視覚路（Ventral Stream）の物体認識が平面解釈を優位にするため、幾何学模様よりも立体視が困難になる（`src/stereogram.js:204` のコメントで警告）。

---

## 3. システムアーキテクチャ

```
[深度ソース] → [深度マップ正規化] → [パターン生成] → [視差変調合成] → [Canvas出力]
      ↓                ↓                  ↓                ↓
  画像ファイル    Coverスケーリング   Random Dots      非累積シフト
  テキスト描画    1000×600 へ正規化  Stripes/Custom    patternWidth基準
  Otsu自動閾値    輝度→深度変換      カラーブレンド    effectiveMaxDepth制限
```

*   **フロントエンド:** `HTML5 / CSS3 / Vanilla JS`（フレームワーク非依存）、`Vite 5.4` で `dist/` にバンドル
*   **画像処理コア:** ブラウザ標準 `Canvas API` のみ。`OffscreenCanvas` ではなく通常の `HTMLCanvasElement` を `document.createElement('canvas')` で生成し、`getContext('2d')` → `getImageData` → `putImageData` の同期パスで完結。サーバーレスで `local-materials/` には触れない。
*   **非同期制御:** `FileReader` ではなく `URL.createObjectURL(file)` → `Image` → `onload` でデコード。`async/await` で逐次処理。

---

## 4. 深度マップ（Depth Map / Height Field）の生成と正規化

### 4.1 定義

深度マップ `D(x,y) ∈ [0,255]` は、出力画像 `outW=1000, outH=600`（`src/stereogram.js:175` 固定）上の各ピクセルにおける **相対深度（Relative Depth）** を8bit輝度で符号化したもので、慣例として

*   `D = 0`（黒, `lum = 0`）: 最遠点（Far Plane, 基準面）
*   `D = 255`（白, `lum = 255`）: 最近点（Near Plane, 最も手前に浮く）

### 4.2 画像モード

`src/stereogram.js:194` で `depthImg`（`loadImage:5` で `URL.createObjectURL` された任意画像）を **Coverスケーリング**（`ratio = max(outW/imgW, outH/imgH)`、`drawImage((outW-w)/2, (outH-h)/2, w, h)`）で `1000×600` に引き伸ばし、`getImageData` 後に `getLum:14` の **Rec.709 輝度変換**

```
lum = 0.2126·R + 0.7152·G + 0.0722·B
depth = lum / 255
```

で正規化。Coverによりアスペクト比は保持されつつ、深度マップは画面全体を覆う。

### 4.3 テキストモード

`src/stereogram.js:120` で `textCanvas:800×400` に `fillRect(#000)` → `fillText(#fff, center)`。`fontWeight` は `select#stereo-font:800`、フォントサイズは `measureText` で `w-80` に収まるまで `120→30px` で減衰。`ctx.filter='blur(2px)'` で2pxガウシアンをかけ、深度の急峻な段差（Depth Discontinuity）による **隠面の裂け（Tearing）** と **偽輪郭（False Contour）** を緩和。最終的に `toDataURL()` → `Image` に再デコードして `depthImg` と同等に扱う。白文字が浮く（Near）ため、直感的に「文字が手前に出る」。

---

## 5. パターン（Carrier / Wallpaper）の生成

`patternWidth ∈ [40,140]px`（`input#stereo-width:36`、デフォルト80px）、`patternCanvas:patternWidth×outH`（`src/stereogram.js:206`）

### 5.1 ランダムドット（Random-Dot, デフォルト）

*   背景: `fillRect('#0a0a0f')`（`src/stereogram.js:220`）— 従来の `#e8e8f0` より **高コントラストな黒背景** に変更。黒背景＋白ドットは **マッチング・プリミティブ（Matching Primitive）** の検出が容易で、Panum領域内での対応点探索が安定する。
*   ドット: 2000個の `arc(x,y, 0.5-2.0px)` を `rgb(120-200)` のグレーでランダム配置（`src/stereogram.js:224`）、さらに400個の `hsl(200-260°, 70%, 60%)` の2px矩形を重畳。前者は **高周波ランダムテクスチャ** として視差検出の手がかり（High-Frequency Carrier）、後者は色の手がかりとして輻輳を誘導する。

### 5.2 ストライプ

`x%8<4 ? rgb(230) : rgb(40)` の垂直二値ストライプ（`src/stereogram.js:239`）。ウォールペーパー錯視の最小構成で、周期が明確なため初心者でも立体視しやすいが、単調なため奥行きの豊かさはドットに劣る。

### 5.3 カスタム

`patternImg`（`loadImage:5`）を同様にCoverで `patternWidth×outH` に描画。任意のシームレス・テクスチャを搬送波にできる。

### 5.4 カラーモード（テクスチャード・ステレオグラム）

`checkbox#stereo-color:34` がONかつ `depthImg` が画像モードの場合、`src/stereogram.js:209` で `depthImg` 自体を `patternWidth×outH` にCover描画し、さらに `rgba(255,255,255,0.06)` の1pxノイズを300個重畳。後の合成段階（`src/stereogram.js:284`）で深度の高い画素（`depth > 0.25`）に限り、パターン画素と元画像画素を `alpha = (depth-0.25)/0.75 * 0.7` で線形ブレンドする。これにより、浮かび上がる部分に限り **元画像のクロミナンス（Chrominance）が保持** され、「カラーのまま浮く」テクスチャード・ステレオグラムになる。OFF時はモノクロのドットのみ。

---

## 6. 視差変調合成 — 非累積方式（Non-Cumulative, Period-Preserving）

本ツールの中核。従来の累積方式（`out[x] = out[x - patternWidth + shift]` で既に歪んだ `out` を再参照）は、1ピクセルの計算誤差 `ε` が `x` 方向に `ε·(outW/patternWidth)` 倍に増幅され、右側で **パターンの崩壊（Pattern Collapse）** と **周期の消失（Period Loss）** を招く（`src/stereogram.js:256` のコメントで指摘）。本実装では **非累積・周期一定** 方式を採用する。

### 6.1 パラメータ

*   `patternWidth`: 基礎周期 `T`（眼間距離に対応する網膜上の周期）
*   `maxDepth`: ユーザ指定の最大視差量 `Dmax ∈ [6,48]px`（`input#stereo-depth:34`, デフォルト24px）
*   `effectiveMaxDepth = min(Dmax, floor(T·0.45))`（`src/stereogram.js:260`）— `Dmax` が `T` の45%を超えると、遠近の分離 `S = T - Dmax` が `T/2` を割り、 **折り返し（Aliasing）** と隠面の過剰な圧縮を防ぐための **安定性拘束（Stability Constraint）**。

### 6.2 水平視差の付与

出力画像 `out(x,y)` の各ピクセルについて、深度 `D(x,y) = lum(depthData[x,y])/255`（`src/stereogram.js:276`）から水平シフト量

```
shift(x,y) = round( D(x,y) · effectiveMaxDepth )   ∈ [0, effectiveMaxDepth]
```

を算出し、搬送波パターン `P` 上の参照位置

```
patternX(x,y) = ( x − shift(x,y) ) mod T
```

でサンプリングする（`src/stereogram.js:280`）。

```js
let patternX = (x - shift) % patternWidth;
if (patternX < 0) patternX += patternWidth * Math.ceil(-patternX / patternWidth);
```

*   **非累積性:** `P` は常に初期に生成された `patternCanvas`（`patternData:254`）を参照し、既に歪んだ `out` を再帰的に参照しないため、誤差が累積しない。
*   **周期一定性:** `T` は全画面で一定。`shift` は `T` 内での位相変調（Phase Modulation）に留まるため、右端まで **ウォールペーパーの繰り返しが等間隔** に保たれる。
*   **幾何学的意味:** `shift` は網膜上での **交差視差（Crossed Disparity, 手前）** に対応。`D=1` で `patternWidth` 方向に `effectiveMaxDepth` だけ **鼻側（Nasal）** にずれるため、両眼が `T` の整数倍＋ `shift` で輻輳した際に `depth ∝ shift` の奥行きが復元される。

### 6.3 カラーブレンド（Color Retention）

`useColor && depth > 0.25` の画素では、パターン画素 `P[patternX]` と元画像画素 `C[x,y]`（`colorData:271`、同位置の `depthImg` を `outW×outH` にリサイズしたもの）を

```
alpha = min(1, (depth - 0.25)/0.75 * 0.7)
out.rgb = P.rgb·(1−alpha) + C.rgb·alpha
```

でブレンド（`src/stereogram.js:284`）。深度0.25以下（遠景）はパターンのまま、深度1.0（最近）で最大70%が元画像の色になるため、浮かび上がる文字や物体が **固有色（Intrinsic Color）** を保ったまま立体視される。

### 6.4 擬似コード（全体）

```js
// 深度マップ D: outW×outH, 0..1
// パターン P: T×outH, RGB
// 出力 O: outW×outH
effectiveMaxDepth = min(maxDepth, floor(T*0.45));
for y in [0, outH):
  for x in [0, outW):
    D = lum(depthData[y*outW+x]) / 255;
    shift = round(D * effectiveMaxDepth);
    px = (x - shift) % T; if(px<0) px+=T;
    O[x,y] = P[px,y];
    if (useColor && D>0.25) O[x,y] = lerp(O[x,y], C[x,y], (D-0.25)/0.75*0.7);
```

計算量は `O(outW·outH)` で、1000×600では60万画素×定数時間、メインスレッドでも100ms未満。`getImageData` / `putImageData` の `Uint8ClampedArray` 走査のみで `Worker` は不要。

---

## 7. UIと状態管理

*   **深度ソース切替:** `button#stereo-mode-image/text:22` で `useTextMode` フラグを反転し、`div#stereo-image-mode/text-mode` の `hidden` を切替（`src/stereogram.js:51`）。`updateGenerateBtn:93` で入力の有無に応じて `button#stereo-generate` の `disabled` を制御。
*   **ファイル入力:** `label.dropzone > input.file-input-hidden`（`src/style.css:1` の `position:absolute; clip:rect(0,0,0,0)` による視覚的隠蔽）を `click` で `input.click()` にフォールバック（`src/stereogram.js:101`）。`dragover` で `classList.add('dragover')` による `border-color: var(--accent)` の強調。
*   **プレビュー:** 生成前は `div#stereo-placeholder`、生成後は `canvas#stereogram-canvas`（`width=outW, height=outH`）を `classList.remove('hidden')`。ズームは `let zoom=1`（`src/stereogram.js:157`）を `style.width = zoom<=1 ? '100%' : zoom*100+'%'` で実現。`src/style.css:1` の `.canvas-wrapper` は `max-height: min(68vh,720px); overflow:auto` でスクロール可能。
*   **保存:** `canvas.toBlob('image/png')` → `URL.createObjectURL` → `a[download]` の一時リンク方式（`src/stereogram.js:314`）。透過は不要なため `alpha=255` 固定。

---

## 8. 立体視の方法（User Guidance）

*   **平行法（Parallel Viewing, 推奨）:** 画面から30cm離れ、焦点を画面の奥（無限遠）に合わせる。左右眼の視線が平行に近づき、 `T` の隣接する周期が網膜上で重なる。数秒で中央に **虚像（Virtual Image）** が浮く。`src/stereogram.js` の `outW=1000, T=80, Dmax≈24` は、視距離30cm・画面解像度96dpiで視差角約0.4°となり、Panum領域内で快適。
*   **交差法（Cross-Eyed）:** 寄り目で手前に輻輳させる方法では奥行きが反転（凹む）するため、本ツールでは非推奨。ドキュメントでは平行法のみを案内（`index.html:343` の `.card-desc`）。

---

## 9. 制約・トレードオフ・アーティファクト

| 項目 | 制約 | 対策 |
|---|---|---|
| **隠面（Occlusion）** | 急峻な深度段差で、手前の物体に隠される背景画素が存在し、単純な `x-shift` では裂け目が生じる | `blur(2px)`（`src/stereogram.js:141`）で深度段差を緩和、将来的には **隠面塗りつぶし（Hidden-Surface Fill）** で `depth` の不連続点を検出し、補間で埋める |
| **偽輪郭（False Fusion）** | ランダムドットの周波数成分が深度のエッジと干渉し、存在しない面が知覚される | 高コントラストな黒背景白ドット（`src/stereogram.js:220`）で搬送波の **自己相関（Autocorrelation）** を下げ、偽対応を抑制 |
| **意味的干渉（Semantic Interference）** | キャラクターのような有意味画像を搬送波にすると、腹側路の物体認識が平面解釈を優位にし、立体視が困難 | ドット/ストライプのような **無意味幾何学テクスチャ** を推奨（`src/stereogram.js:204` のコメント） |
| **視差の上限** | `Dmax > T/2` で周期の半分を超える視差は `T-Dmax` と等価され、ため、偽と `視Depth�on++?奥行きの折り返し` が起こる | `effectiveMaxDepth = min(Dmax, T*0.45)` で上限を保証（`src/stereogram.js:260`） |
| **解像度依存性** | `outW=1000` 固定のため、4Kディスプレイでは1pxが物理的に小さく、視差角が不足する | 将来的に `outW` を `window.devicePixelRatio` に応じて可変にする |

---

## 10. デバッグ手法

`src/stereogram.js:256` のコメントにある通り、複雑な深度でデバッグせず、**搬送波にランダムドット、深度に中央の白い四角（例: 400×200）** のみでテストする。白四角が手前に浮く四角錐台として知覚されれば、アルゴリズムは正常。右側がノイズ化する場合は、非累積方式が守られていないか、`effectiveMaxDepth` が `T` を超えていないかを確認する。

---

## 11. 今後の拡張

*   **隠面処理の精密化:** 深度の勾配 `∇D` から隠面幅を算出し、 `inpainting` で背景を補完
*   **動的立体視（Animated Stereogram）:** `requestAnimationFrame` で `D` を時間変化させ、波打つ立体を生成
*   **印刷用CMYK変換:** `Canvas` の `toBlob('image/png')` のままでは印刷時の網点と干渉するため、`patternWidth` を印刷解像度（300dpi）で再計算

---

*本書は `src/stereogram.js:1` の実装を一次情報とし、立体視の生理学は Tyler (1990), Julesz (1971), Marr & Poggio (1979) に、ウォールペーパー錯視の数理は `patternWidth` と `shift` の剰余演算に依拠する。*
